parameters:
  workingDirectory: ''
  backendConfig: ''
  command: ''  # validate, plan, apply, or destroy

steps:
- task: ms-devlabs.custom-terraform-tasks.custom-terraform-installer-task.TerraformInstaller@1
  displayName: 'Install Terraform'
  inputs:
    terraformVersion: 'latest'

- task: AzureCLI@2
  displayName: 'Run Terraform Command'
  inputs:
    azureSubscription: $(AZURE_SUBSCRIPTION)
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    workingDirectory: ${{ parameters.workingDirectory }}
    inlineScript: |
      set -e

      echo "Writing backend config to file..."
      echo "${{ parameters.backendConfig }}" > backend.tfbackend

      echo "Initializing Terraform..."
      terraform init -backend-config=backend.tfbackend

      case "${{ parameters.command }}" in
        validate)
          terraform validate
          ;;
        plan)
          terraform plan -out=tfplan
          ;;
        apply)
          terraform apply -auto-approve tfplan
          ;;
        destroy)
          terraform destroy -auto-approve
          ;;
        *)
          echo "Invalid command: ${{ parameters.command }}"
          exit 1
          ;;
      esac