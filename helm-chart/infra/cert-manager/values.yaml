# Cert-Manager Infrastructure Configuration
cert-manager:
  enabled: true
  global:
    leaderElection:
      namespace: cert-manager

# Production ClusterIssuer
productionIssuer:
  enabled: true
  name: "letsencrypt-prod"
  email: "c.vanlare@outlook.com"  # Must be provided via --set productionIssuer.email or from Key Vault
  server: "https://acme-v02.api.letsencrypt.org/directory"
  ingressClass: "nginx"

 