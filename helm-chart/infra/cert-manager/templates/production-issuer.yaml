{{- if .Values.productionIssuer.enabled }}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ .Values.productionIssuer.name }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-weight: "5"
spec:
  acme:
    server: {{ .Values.productionIssuer.server }}
    email: {{ .Values.productionIssuer.email }}
    privateKeySecretRef:
      name: {{ .Values.productionIssuer.name }}
    solvers:
    - http01:
        ingress:
          class: {{ .Values.productionIssuer.ingressClass }}
{{- end }} 